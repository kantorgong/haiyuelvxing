<?php
namespace common\models\redis;

use Yii;
use \yii\redis\ActiveRecord;

class LotteryBehaviorLog extends ActiveRecord
{
    public function attributes()
    {
        return ['id', 'key', 'x', 'y', 'diff', 'page', 'token', 'time', 'open_id', 'coordinate', 'duration'];
    }
}
